.env.local
node_modules
dist

# Convex generated types ARE committed so static hosts (Render) can build
# without running Convex codegen during the build. They are deterministic
# output of `bunx convex dev --once` / `bunx convex codegen`.
